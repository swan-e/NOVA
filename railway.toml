[build]
dockerfile = "Dockerfile.upload"

[deploy]
startCommand = "node build/upload-server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3